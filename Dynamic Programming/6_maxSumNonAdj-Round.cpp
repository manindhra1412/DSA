// Same as previous question with a slight modification

// So first and last houses/elements are adjacent so we shouldn't include them both in one

// So answer=max(ans without including first element,ans without including first element);